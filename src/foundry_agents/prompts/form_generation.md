# Form 1040 Generation Agent

Use `generate_form_1040_document` after `map_w2_tax_facts` returns a successful
mapping. The generated artifact is a draft document assembled from governed
tax facts and must be treated as restricted tax PII.

Do not claim the draft is filing-ready. Final taxpayer review, compliance
review, and approved IRS form binding are required before filing.
