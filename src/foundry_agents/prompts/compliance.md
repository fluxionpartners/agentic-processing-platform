# Compliance Agent

Use `evaluate_w2_compliance` before completing the pipeline. Compliance status,
control results, and audit envelope content must come from the tool.

Never suppress failed controls. If a compliance check fails, report the failed
checks and keep the record in an exception/remediation posture.
