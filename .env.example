# Local development defaults for the Foundry agent pipeline.
# Copy to .env for local use if your tooling loads .env files, or set these
# values directly in your shell, IDE launch profile, Azure app settings, or
# Foundry hosted agent environment.

APP_ENV=local

# Foundry prompt agent model deployment
AZURE_OPENAI_DEPLOYMENT_NAME=

# Foundry supervisor agent invocation from the deployed tool host
FOUNDRY_PROJECT_ENDPOINT=
FOUNDRY_SUPERVISOR_AGENT_ID=
FOUNDRY_SUPERVISOR_AGENT_NAME=foundry-w2-tax-orchestrator
FOUNDRY_API_VERSION=v1

# Upload portal build-time settings
# direct: portal upload publishes the backend async pipeline event
# foundry-agent: portal upload stages the W-2 and invokes the supervisor agent
# selectable: portal lets the tester choose either path
PORTAL_EXECUTION_MODE=selectable
VITE_W2_INTAKE_API_URL=
VITE_W2_PROCESSING_API_URL=
VITE_W2_STATUS_API_URL=
VITE_W2_AGENT_API_URL=
VITE_W2_EXECUTION_MODE=selectable
VITE_AUTH_ENABLED=false
VITE_AUTH_TENANT_ID=
VITE_AUTH_CLIENT_ID=
VITE_AUTH_SCOPE=

# Extraction
# local: deterministic offline adapter for development/tests
# document-intelligence: Azure AI Document Intelligence adapter
W2_EXTRACTION_MODE=local
AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=
AZURE_DOCUMENT_INTELLIGENCE_KEY=
AZURE_DOCUMENT_INTELLIGENCE_MODEL_ID=prebuilt-tax.us.w2

# Validation
W2_VALIDATION_STRICTNESS=standard
LOW_CONFIDENCE_THRESHOLD=0.85

# Human review
# local-auto-approve: lets local tests continue
# queue/manual: pauses pipeline until human decision is recorded
HUMAN_REVIEW_MODE=local-auto-approve

# Tax mapping
TAX_MAPPING_PROFILE=us-federal-2024

# Form 1040 document generation
# html-template: deterministic draft artifact generated from mapped tax facts
FORM_1040_GENERATION_MODE=html-template
FORM_1040_TEMPLATE_VERSION=irs-1040-2024-html-v1
# local-file: write draft HTML artifacts under FORM_1040_ARTIFACT_PATH
# azure-blob: write draft artifacts to FORM_1040_BLOB_CONTAINER_NAME
FORM_1040_ARTIFACT_MODE=local-file
FORM_1040_ARTIFACT_PATH=.local_state/form-1040
FORM_1040_BLOB_CONTAINER_NAME=tax-artifacts
FORM_1040_STORAGE_ACCOUNT_URL=
FORM_1040_STORAGE_CONNECTION_STRING=

# Compliance and audit
COMPLIANCE_MODE=development
REQUIRE_MASKED_PII_IN_LOGS=true
AUDIT_EVENT_ENABLED=true

# Governed tax fact persistence
# disabled: do not persist normalized tax facts
# local-json: write governed JSON records under TAX_FACT_PERSISTENCE_PATH for local development
# cosmos: upsert governed checkpoints to Azure Cosmos DB
TAX_FACT_PERSISTENCE_MODE=local-json
TAX_FACT_PERSISTENCE_PATH=.local_state/tax-facts
AZURE_COSMOS_ENDPOINT=
AZURE_COSMOS_DATABASE_NAME=tax-intelligence
AZURE_COSMOS_CONTAINER_NAME=tax-facts
AZURE_COSMOS_KEY=
ALLOW_FULL_PII_PERSISTENCE=false
